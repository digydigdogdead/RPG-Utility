<Window x:Class="RPGUtility.Windows.UnsavedChangesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RPGUtility.Windows"
        mc:Ignorable="d"
        Title="Unsaved Changes" Height="210" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.2*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="You have unsaved changes. Are you sure you want to exit?"
                   FontSize="20"
                   VerticalAlignment="Center"
                   Margin="10"
                   TextWrapping="WrapWithOverflow"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"/>
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Margin="10"
                    >
            <Button x:Name="ExitButton"
                    Background="Red"
                    Content="Exit"
                    Padding="10"
                    Margin="10"
                    Click="ExitButton_Click"/>
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Padding="10"
                    Margin="10"
                    Click="CancelButton_Click"
                    /> 
        </StackPanel>
    </Grid>
</Window>
