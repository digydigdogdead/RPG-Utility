<Window x:Class="RPGUtility.Windows.AddToCalendarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RPGUtility.Windows"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="Add Session To Calendar" Height="300" Width="550">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                <TextBlock FontSize="25" 
                           Text="Event Title:"
                           VerticalAlignment="Center"/>
                <TextBox x:Name="eventTitleTextBox"
                         FontSize="16"
                         Width="200"
                         Margin="5, 0, 0, 0"/>

            </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                <TextBlock FontSize="25" 
                           Text="Year:"
                           VerticalAlignment="Center"/>
                <xctk:IntegerUpDown Value="1" 
                                    x:Name="yearUpDown"
                                    Width="50"
                                    Margin="10"
                                    FontSize="25"/>
            </StackPanel>
            <StackPanel Margin="10" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock FontSize="25" VerticalAlignment="Center"
                           Text="Month:"
                           />
                <ComboBox x:Name="monthComboBox"
                            FontSize="16"
                            Width="150"
                            Margin="10"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <TextBlock FontSize="25" VerticalAlignment="Center"
                           Text="Day:"
                           />
                <xctk:IntegerUpDown Value="1" 
                                    x:Name="dayUpDown"
                                    Width="50"
                                    Margin="10"
                                    FontSize="25"
                                    Minimum="1"/>
            </StackPanel>
        </StackPanel>
        <Button Grid.Column="1"
                x:Name="addToCalendarButton"
                Content="Add"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                FontSize="24"
                Padding="20"
                Background="LightGreen"
                Click="addToCalendarButton_Click"/>
    </Grid>
</Window>
