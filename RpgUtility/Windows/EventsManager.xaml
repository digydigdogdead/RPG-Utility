<Window x:Class="RPGUtility.Windows.EventsManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RPGUtility.Windows"
        mc:Ignorable="d"
        Title="Events Manager" Height="450" Width="700"
        Background="LightGray">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="1 January, 1000"
                   x:Name="titleTextBlock"
                   FontSize="26"
                   Grid.ColumnSpan="2"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1"
                    VerticalAlignment="Center">
            <TextBlock Text="Event:"
                       HorizontalAlignment="Center"
                       FontSize="20"/>
            <TextBox x:Name="eventTextBox"
                     FontSize="18"
                     Margin="6"
                     Height="50"
                     TextWrapping="Wrap"
                     />
            <Button x:Name="addEventButton"
                    Content="+"
                    Click="addEventButton_Click"
                    Margin="5"
                    Background="LightGreen"
                    Height="50" Width="50"
                    FontSize="30"
                    VerticalContentAlignment="Center"/>
            <Button x:Name="removeEventButton"
                    Content="-"
                    VerticalContentAlignment="Top"
                    Click="removeEventButton_Click"
                    Margin="5"
                    Background="Red"
                    Height="20" Width="40"
                    FontSize="15"
                    />
        </StackPanel>
        <ListView x:Name="eventsListView"
                  Grid.Row="1"
                  Grid.RowSpan="2"
                  Grid.Column="1"
                  Margin="10"
                  FontSize="16"
                  />
        <Button x:Name="closeButton"
                Content="Done"
                Click="closeButton_Click"
                Grid.Row="2"
                HorizontalAlignment="Center"
                Width="100"
                Margin="5"/>
    </Grid>
</Window>
